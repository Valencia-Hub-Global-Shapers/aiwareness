name: Sync hubs to Supabase

on:
  push:
    branches: [main]
    paths:
      - "locales/**"

jobs:
  sync-hubs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - name: Sincronizar tabla "hubs" con locales/
        run: node scripts/sync-hubs.js
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
